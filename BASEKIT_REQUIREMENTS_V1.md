# 【Ver.1.0】共通ベースキット ＆ オプションプラグイン 要件定義・仕様書

本仕様書は、手離れが良く、トラブルの起きない頑丈なWebシステム（特に知人や中小規模のクライアント向けシステム）を迅速に構築・納品するための**「コアシェル（ベースキット） ✕ オプションプラグイン」**アーキテクチャの要件および設計仕様を定義したものです。

新規プラグインの開発やベースキットの改修にあたっては、必ず本仕様書の原則（特に疎結合ルール）に準拠してください。

---

## 1. 全体アーキテクチャ ＆ 設計思想

本システムは、**「極限まで薄く頑丈な土台（ベースキット）」**に、**「着脱可能な機能モジュール（プラグイン）」**をアタッチする構成を採用します。

```mermaid
graph TD
    subgraph Base Kit (ベースキット：本体)
        UI_Layout["共通レイアウト & スロット<br>(Header/Sidebar/Portal)"]
        Auth_Gate["認可・免責ゲート<br>(Middleware / 期限管理)"]
        Plugin_Bus["PluginBus<br>(イベント駆動・中継)"]
        Log_Trig["100%自動操作ログ<br>(監査トリガー / セッション伝播)"]
    end

    subgraph Plugins (オプションプラグイン：個別機能)
        Plugin_SNS["SNSタイムライン"]
        Plugin_Task["タスク・売上管理"]
        Plugin_Export["データ退避 (JSON/CSV)"]
        Plugin_Guard["システム自己診断 & 遮断"]
    end

    UI_Layout -->|動的ロード| Plugins
    Auth_Gate -.->|ガードの強制| Plugins
    Plugin_Bus <.->|疎結合通信| Plugins
```

### 3大開発原則
1. **疎結合の徹底 (Decoupling First)**
   ベースキット本体のコード内に、特定のプラグインを直接呼び出す記述（`import` やダイレクトコール）を行ってはなりません。互いの情報は「インターフェース」と「イベントバス」のみを介してやり取りします。
2. **自己防衛（自己完結型セキュリティ）**
   プラグイン側で「同意チェック」や「期限切れチェック」を実装し忘れても、ベースキットのゲートキーパー（MiddlewareおよびDBポリシー）で自動的にアクセスを完全遮断できる二重ロック構造を維持します。
3. **引き算の設計（ミニマリズム）**
   不要な重い外部ライブラリ（npmパッケージ）の導入を避け、Node.jsおよびWeb標準のAPI（Vanilla実装）を中心にコードを極限まで薄く保ち、肥大化を防ぎます。

---

## 2. ベースキット（本体）要件定義

ベースキットは、これ以上削れない「システムの骨組みとセキュリティの壁」のみを提供します。

### 2.1. 共通UIレイアウト ＆ スロット接続口
* **ページ型マウントスロット (Dynamic Pages)**:
  - `/plugins/[pluginId]` という動的URLを提供し、対応するプラグインコンポーネントを動的にインポート（`next/dynamic`, `ssr: false`）して描画する口を用意します。
  - プラグインが追加されても、ベース側のルーティング定義ファイルを変更する必要はありません。
* **メニュー動的生成 (Dynamic Sidebar)**:
  - 有効化されているプラグインのメタデータ（名称、アイコン、パス）を読み込み、サイドバーのメニューリンクを動的に生成します。
* **ウィジェット型マウントスロット (Widget Slots)**:
  - ダッシュボードやヘッダー等の所定の領域に、プラグイン独自のウィジェット（エラーバッジ、サマリー表示など）を差し込める口をプロバイダー経由で提供します。

### 2.2. 共通権限 ＆ 認可インフラ
* **共通ユーザー権限マスタ (`plugin_user_ext_status` 相当)**:
  - ユーザーID、ロール（`admin` / `member`）、およびアクセス有効期限（`expires_at`）を一元管理します。
* **Middleware二重ロックゲート**:
  - 未同意ユーザー、および有効期限切れのユーザー（`expires_at` が過去）からのアクセスを、ページ・APIリクエストの双方で強制遮断（ログイン/同意画面へリダイレクト、または `403 Forbidden` 返却）します。
* **DBポリシー（RLS / Prismaラッパー）の共通化**:
  - データベース層において、有効期限内ユーザーのみが自身の所有データにアクセスできるよう、一元的なセキュリティポリシーを強制します。

### 2.3. プラグインバス (`PluginBus`)
* 疎結合なイベント駆動を実現する中継器です。
* **耐障害性仕様**: 
  - イベント発火（`emit`）時は、リスナーを `Promise.allSettled` で並列に処理します。
  - 1つのプラグインでエラーやタイムアウトが発生しても、本体の処理や他の正常なプラグインの処理を巻き添えでクラッシュさせない設計とします。

### 2.4. 100%自動操作ログ（監査ログ）
* DB（PostgreSQLのトリガー関数、またはSQLiteのPrismaミドルウェア）を用いて、全CRUD操作の履歴（「いつ・誰が・何を・どう変更したか」）を自動記録します。
* アプリケーションの開始時にセッション情報をDBへ伝播させ、APIコード側のログ記述漏れを完全に排除します。

---

## 3. プラグイン（オプション）要件定義

すべての個別機能は「オプションプラグイン」として完全にカプセル化（自己完結）して開発します。

### 3.1. プラグインの構成ファイル定義
プラグインは、`src/plugins/[pluginId]/` フォルダ内に以下のファイルを自己完結的に持ちます。

* **`meta.ts` (プラグインマニフェスト)**:
  - プラグイン名、ID、アイコン、有効/無効フラグ、および**セキュリティ区分（`networkRequired: boolean`）**を定義。
* **`index.tsx` (UIコンポーネント)**:
  - 画面に表示するUIおよびフロントエンドロジック。
* **`api/` (専用APIハンドラー)**:
  - ベースキットの動的APIゲートウェイ（`/api/plugins/[pluginId]/`）を介して呼び出される、プラグイン固有のバックエンドロジック。
* **`schema.sql` / `schema.prisma` (DBスキーマ断片)**:
  - プラグインが独自に必要とするテーブル定義。

### 3.2. プラグイン疎結合設計ルール
* **直接参照（Import）の禁止**:
  - ベースキット本体の共通コード（例: `src/app/page.tsx` や `src/middleware.ts`）から、プラグインフォルダ内のファイルを直接 `import` してはなりません。
* **スキーマの分離**:
  - プラグイン独自のテーブルは、コアのテーブル（`users` 等）とデータベース層でのFOREIGN KEY等による強固な制約を結ばず、単なる文字列/数値IDによる「論理的なリレーション」にとどめます。
* **プレフィックスの付与**:
  - DBテーブル名、およびブラウザの `localStorage` キーには、衝突を防ぐため必ず `plugin_[pluginId]_` というプレフィックスを付与します。

---

## 4. オプションプラグイン（Ver.1想定リスト）

基本機能（ベースキット）の上に着脱可能なオプションとして設計・開発するプラグインの定義です。

### ① 【業務】SNSタイムラインプラグイン
* **用途**: クローズドな社内SNS・タイムライン機能。
* **永続化**: クラウド型（Supabase/PostgreSQL）またはローカル型（Prisma/SQLite）の共通DBアダプタを利用。
* **機能**: 投稿作成、いいね（リアクション）、ファイル添付（サイズ/拡張子制限付き）。

### ② 【業務】noteメンバーシップ認証プラグイン
* **用途**: noteの共通暗号トークンを検証し、アクセス有効期限（`expires_at`）を更新する。
* **永続化**: 認証トークンマスターテーブルの管理。

### ③ 【業務】個人タスク・売上管理プラグイン
* **用途**: 個人専用の日々のタスク管理および売上集計・カウンター。
* **機能**: タスクCRUD、日報自動生成（印刷CSS対応）、カレンダーUI連携。

### ④ 【運用】データ退避（ポータビリティ）プラグイン
* **用途**: 開発者がサポートに入ることなく、クライアント自身でデータを管理・退避・移行できるようにする。
* **機能**: 
  - **データエクスポート**: 全テーブルデータを単一のJSON/CSVファイルとしてワンクリックでダウンロード。
  - **データインポート**: ダウンロードしたJSONをドラッグ＆ドロップし、データを安全にマージ・復元（ローカル保存/SQLite双方に対応）。

### ⑤ 【保守】システム自己診断プラグイン
* **用途**: 障害発生時の問い合わせ対応コストをゼロに近づける。
* **機能**:
  - 起動時またはオンデマンドで、DB接続、ストレージ空き容量、プラグイン稼働状況の整合性をチェック。
  - 異常検知時、管理画面に警告マーク（⚠️）を表示。
  - **ワンクリックログ出力**: 直近のエラーログや環境情報をマスキングした診断レポート（JSON/テキスト）をダウンロード可能にする。

### ⑥ 【防衛】簡易サーキットブレーカー（自動遮断）プラグイン
* **用途**: 特定の単機能システムや外部APIがクラッシュした際に、他の正常なシステムを守る。
* **機能**:
  - `/api/health` 経由で各単機能ユニットの死活状態を監視。
  - 異常を検知した接続先へのデータ送信やAPIリクエストを一時的に**自動遮断（隔離）**。
  - 遮断中、UI側には自動で「調整中」のフォールバック表示を差し替える。

---

## 5. バージョンアップ・変更管理

* **Ver.1.0**: 本ドキュメントにて定義した「コア ✕ プラグイン」の役割定義および疎結合化の基本仕様をマスターとします。
* **今後の拡張計画 (Ver.1.1以降)**:
  - 実際に部門別システム（SNSやタスク管理等）を構築・結合していく中で、データ同期方式（パターンA/B/C）のより具体的なAPIスキーマや、共通認証（SSO/JWT）仕様が確定した段階で、本仕様書をアップデートします。
