# business_sns_kit - 統合開発計画書

本ファイルは、AIがプロジェクトの要件、構成、進捗を同期するためのマスター指示書です。開発時は常に本ファイルを読み込ませて開発を継続してください。

---

## 1. プロジェクト概要 ＆ ゴール
* **開発の目的**：個人事業主や中小企業等のクライアントからの開発依頼・相談に備え、社内専用のクローズドコミュニケーションを実現するSNSシステムを効率的に構築・納品するためのベースキットとして開発する。
* **成果物**：クライアント向けの社内専用クローズドSNSシステム（Next.js + Supabase/PostgreSQL）。およびスピンオフ機能としてのnoteメンバーシップマンスリー認証（月次期限管理）。
* **防衛方針**：クライアントとの不要な法的トラブル（損害賠償リスク、夜間呼び出し等）を回避し良好な関係を守るため、免責ゲートおよび承諾書によるシステム・運用面での多重ガードを徹底する。
* **共通化設計方針**: 
  noteメンバーシップの「月次期限管理」と、業務利用における「入退社（新規・退職者）のアクセス制御」は、技術的に同一の「`expires_at`（有効期限切れ）による制御（Supabase RLS）」として共通化して定義する。これにより、アクセス制限のロジックを一本化し、堅牢で疎結合なセキュリティ設計を維持する。

---

## 2. System要件 ＆ 拡張モジュール

### 【コア機能（必ず動く土台）】
* **タイムライン・モジュール**：
  - テキスト投稿、画像・ファイル添付（保存先：Supabase Storage）。
  - リアクション機能（いいね・解除 of トグル制御。`plugin_sns_reactions` テーブルで個別管理）。
  - リアルタイム同期（WebSocket/Supabase Realtimeによるリロードなしの反映）。
  - 初期フェーズでは単一タイムラインのみ（チャンネルやDMは対象外）。
* **アクセス期限制御（共通ゲートキーパー）**：
  - `plugin_user_ext_status` テーブルの `expires_at` フィールドを用いた一元的なアクセス制御。
  - **ユースケースA (noteメンバーシップ)**: 正しいトークンをタップした際、`expires_at` を「翌月末まで」に更新。
  - **ユースケースB (社内SNS)**: 新規入社時に権限を付与し、退職日や契約満了日を `expires_at` に設定することでアクセスを自動遮断。
  - 有効期限切れのユーザーに対しては、Supabase RLSによりデータ参照・書き込みを完全遮断し、フロントエンド側でもタイムラインを非表示にして案内画面に強制リダイレクトする。
* **免責ライセンスゲート**：
  - [BASEKIT_SPECIFICATION.md](file:///C:/Users/tk030/Desktop/アクティブ/business_sns_kit/BASEKIT_SPECIFICATION.md) に準拠した、起動時の完全免責同意とAPI/画面の二重ロック機能。
* **100%自動操作ログ（監査ログ）**：
  - [BASEKIT_SPECIFICATION.md](file:///C:/Users/tk030/Desktop/アクティブ/business_sns_kit/BASEKIT_SPECIFICATION.md) に準拠した操作ログ記録。Supabase/PostgreSQLのトリガー関数を用いて自動記録。
* **プラグイン接続インターフェース（DI ＆ Webhook）**：
  - 起動時に外部（母体システム）から `userId`, `userName`, `avatarUrl` を注入（DI）可能。
  - 外部システムからの通知を受け取り、タイムラインに自動投稿を代行するWebhook窓口を提供。

### 【同梱する拡張モジュール（src/modules/）】
* **noteメンバーシップマンスリー認証（スピンオフ機能）**：
  - URL `https://[ドメイン]/verify?code=[当月の共通暗号トークン]` 経由でアクセスしたユーザーに対し、トークンの有効性を検証のうえ `expires_at` を自動更新するモジュール。

---

## 3. フォルダ・ファイル構成（システムの間取り図）

```plaintext
business_sns_kit/
├── PROJECT_PLAN.md            # 本計画書（AIへの指示用マスターファイル）
├── BASEKIT_SPECIFICATION.md   # 共通設計パターン＆防衛アーキテクチャ仕様書
├── README.md                  # 使い方マニュアル（利用者向け）
├── schema.sql                 # DB設計図（Supabase用DDL、RLS、トリガー定義）
├── .env.example               # 環境変数とモジュールON/OFF用の設定スイッチ
├── check-no-network.js        # 外部通信混入チェッカー（Node.js製）
├── .cursorrules               # AIエージェントへのルール設定
├── .gitignore                 # Node/Next.js/Supabase用Git除外設定
│
├── ai_pipeline/               # ★【パイプライン連携用成果物フォルダ】
│   ├── result.json            # 実行結果（自動出力）
│   ├── test_report.md         # テスト結果（自動出力）
│   ├── log.md                 # 開発ログ・判断記録（自動出力）
│   └── development_chat_history.md # 開発チャット履歴（自動出力）
│
├── src/
│   ├── lib/                   # バックエンド共通ロジック（接続、イベントバス等）
│   │   ├── supabase.ts        # Supabaseクライアント接続
│   │   └── plugin-bus.ts      # モジュールへイベントを伝える中継器
│   │
│   ├── app/                   # 画面 ＆ API ルート
│   │   ├── page.tsx           # SNSタイムライン画面（メインダッシュボード）
│   │   ├── login/
│   │   │   └── page.tsx       # 免責ライセンスゲート画面（同意ロック）
│   │   ├── verify/
│   │   │   └── page.tsx       # noteメンバーシップ認証用検証ページ
│   │   └── api/
│   │       ├── posts/
│   │       │   └── route.ts   # 投稿・リアクション関連API（セキュリティ検証付き）
│   │       └── webhook/
│   │           └── route.ts   # 外部通知受付API（システム投稿用）
│   │
│   └── modules/               # 拡張モジュール
│       └── note-auth/         # noteメンバーシップ用認証ロジック・プラグイン
```

---

## 4. 工程管理票（進捗ステータス管理）
 
* **全体進捗率**: ▓▓▓▓▓▓▓▓▓▓▓ 100% (13/13 ステップ完了)
* **最終更新日**: 2026年6月28日
* **防衛方針**: クライアントとの良好な関係を守るため、免責ゲートおよび承諾書による多重ガードを徹底する。
 
| フェーズ | ステップ | タスク内容（練習 ＆ 自己防衛） | 対象ファイル | ステータス | 担当 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **P1: 準備** | 1-1 | フォルダ・ファイル構成の確定とテンプレート配置 | `PROJECT_PLAN.md`<br>`BASEKIT_SPECIFICATION.md` | **[完了]** | AI |
| | 1-2 | Git初期化とファーストコミット | `.gitignore`, `git init` | **[完了]** | AI |
| | 1-3 | 環境変数・共通設定ファイルの定義 | `.env.example` | **[完了]** | AI |
| **P2: 裏側** | 2-1 | Supabaseスキーマ、RLSポリシー、監査ログトリガー作成 | `schema.sql` | **[完了]** | AI |
| | 2-2 | 免責ゲート＆API二重ロック作成 | `middleware.ts`, `/login` | **[完了]** | AI |
| | 2-3 | タイムラインAPIおよびログセッション連携作成 | `src/app/api/posts/route.ts` | **[完了]** | AI |
| **P3: 画面** | 3-1 | 基本デザインと共通レイアウト作成（権限管理およびスマホ対応を含む） | `src/app/layout.tsx`<br>`src/app/globals.css`<br>`schema.sql` | **[完了]** | AI |
| | 3-2 | タイムライン画面（投稿・表示・リアルタイム同期）作成 | `src/app/page.tsx` | **[完了]** | AI |
| | 3-3 | リアクション＆ファイル添付インターフェース実装 | `src/app/page.tsx` | **[完了]** | AI |
| **P4: 拡張** | 4-1 | noteメンバーシップマンスリー認証モジュールの作成 | `src/modules/note-auth/` | **[完了]** | AI |
| | 4-2 | 認証URL検証ページの実装 | `src/app/verify/page.tsx` | **[完了]** | AI |
| **P5: 仕上げ**| 5-1 | 使い方マニュアル（README）の作成 | `README.md` | **[完了]** | AI |
| | 5-2 | 外部通信静的スキャン（セキュアテスト）の実施 | `check-no-network.js` | **[完了]** | AI |

---

## 5. 成功条件（DoD）
- [x] 正常動作（Supabaseとの連携、タイムラインのリアルタイム投稿・リアクション、期限切れによる遮断が動作すること）
- [x] 仕様一致（社内SNSを主軸に、期限切れ制御を共通化し、note認証をスピンオフとして実装できていること）
- [x] エラー制御がなされている（無効な暗号トークン、期限切れ、DB接続エラーのハンドリング）
- [x] 成果物（`ai_pipeline/result.json`, `ai_pipeline/test_report.md`, `ai_pipeline/log.md`, `ai_pipeline/development_chat_history.md`）が `ai_pipeline/` フォルダに自動出力されていること

---

## 6. 運用アセット：ソフトウェア利用に関する承盛書（テンプレート）

```plaintext
【ソフトウェア利用に関する承諾書】

本ソフトウェア（以下「本システム」という）は、クライアントごとの個別要件に最適化して提供される、MITライセンスベースの個別システムです。

（無保証） 利用者は、本システムに不具合やバグが存在する可能性があることを理解し、現状有姿で利用するものとします。
（免責） 制作者は、本システムの利用、または利用不能によって生じた損害（データの消失、業務の中断、営業利益の損失などを含むがこれらに限定されない）について、一切の法的責任および賠償責任を負わないものとします。
（ライセンス） 本システムの著作権は制作者に帰属し、MITライセンスに基づいて提供されます。

署名日：2026年 ＿月 ＿日
利用者氏名（サイン）：＿＿＿＿＿＿＿＿＿＿＿
```

---

## 7. 現在地と次回のタスク
* **【現在地】**：全フェーズ（P1〜P5）の全ステップ完了
* **【次回タスク】**：なし（システムリリース・引渡し準備完了）
